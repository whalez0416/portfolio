# 게임 기획서

이 문서는 **게임이 어떻게 돌아가는가**를 적습니다.
이게 없어서 한 번 사고가 났습니다 — 서사만 보고 옛날 엔진에 얹는 바람에
확정된 시점(등 뒤)이 반영되지 않은 채로 커밋이 여덟 개 올라갔습니다.

## 문서 순서

| 순서 | 문서 | 무엇을 정하나 |
|---|---|---|
| 1 | `docs/narrative.md` | **무슨 이야기인가.** 열 장면, 화면에 뜰 문장 |
| 2 | `docs/programs.md` | 문 여덟 개 안에서 무슨 말을 하나 |
| 3 | `docs/design/` | **어떻게 보이나.** 화풍 B, 인물 러닝 사이클 |
| 4 | **이 문서** | **어떻게 돌아가나.** 시점·조작·배치·규칙 |
| 5 | `index.html` | 코드 |

**위에서 아래로만 흐릅니다.** 코드가 문서를 이기지 않습니다.
이 문서와 코드가 다르면 코드가 틀린 겁니다.

---

## 한 줄

> **등 뒤에서 보는 시점 하나로 90초를 달립니다.
> 길이 제 12년이고, 길 위의 문 여덟 개가 제가 만든 것들입니다.**

장르는 템플 런 계열의 **오토러너**입니다. 달리는 건 게임이 합니다.

---

## 1. 시점 — 확정, 그리고 절대 바뀌지 않음

**등 뒤에서 보는 3인칭 원근 시점 하나.** 처음부터 끝까지.

이전 버전은 1~5장을 위에서 내려다보다가 갈림길에서 옆으로 눕혔습니다.
**두 개의 게임처럼 느껴진다**는 이유로 폐기됐습니다. 다시 만들지 마세요.

```
proj(d) = { y: HORIZON + CAM_H/d*F,  hw: ROAD_HW/d*F,  s: F/d }
W 960 · H 540 · HORIZON 0.42H · F 570 · CAM_H 1.85 · ROAD_HW 3.6 · NEAR 1.7
```

- 세상의 모든 것은 `(x, z)` **월드 단위**입니다. 픽셀이 아닙니다.
- **크기는 반드시 `월드크기 × p.s`.** `p.s / F * 40` 같은 식은 가까울수록 폭발해서
  제일 앞 가로등이 풍선만 해집니다. 이건 실제로 한 번 났던 사고입니다.
- 카메라 가로 위치는 주인공을 **따라가되 늦게** 따라갑니다(`3.4/s`).
  안 그러면 주인공이 화면 정중앙에 못박혀서 방향을 틀어도 아무 일도 안 일어난 것처럼 보입니다.
- **등 뒤 시점이므로 지나온 것은 존재하지 않습니다.** 발행한 페이지를 앞쪽 40 단위에
  놓는 것도, 안내 문구가 앞을 가리키는 것도 전부 이 한 줄에서 나옵니다.

## 2. 조작 — 두 개뿐

| 키 | 하는 일 | 언제부터 |
|---|---|---|
| `←` `→` | 방향 | 처음부터 |
| `↓` | 발행 | **첫 번째 문(aeo_sync) 이후** |
| `Enter` / 앰버 버튼 탭 | 지나온 문 읽기 | 문을 지난 뒤, 선택 |
| `Esc` | 읽던 문 닫기 | |

**점프 없음.** 앞으로 가는 키도 없습니다 — 달리는 건 알아서 합니다.
어려우면 안 된다는 게 전제라, 손가락으로 할 일을 두 개로 묶었습니다.

터치는 `◀ ▶ ▼발행` 세 버튼. **발행 버튼은 첫 문 전까지 비활성**이지만,
방향 버튼은 **절대 비활성화하지 마세요** — 예전에 갈림길 조작을 막았다가
터치 사용자가 영구히 갇힌 적이 있습니다.

## 3. 실패가 없습니다

죽지 않고, 리셋이 없고, 점수로 지지 않습니다.
막히는 곳은 딱 두 군데인데 둘 다 **방향만 틀면 풀립니다**:

- **갈림길**(z 1090): 왼쪽으로 안 내려서면 포장도로가 끝나 멈춥니다.
- **국경**(z 2260): NetTier 문을 안 지났으면 못 넘어갑니다. (지나면 자동 해제)

자기소개라서 그렇습니다. 못 보고 끝나는 이력서는 이력서가 아닙니다.

## 4. 길 · 문 · 기록 — 섞지 않습니다

| | 무엇 | 출처 | 강제되나 |
|---|---|---|---|
| **길** | 서사. 달리면 열 장면이 흐름 | `docs/narrative.md` | 강제 |
| **문** | 프로그램. 만든 이유·막힌 곳 | `docs/programs.md` | **선택** |
| **기록** | 사실. 수치와 스택 | `index.html`의 `.rec` 한 벌 | 아래 본문 |

- 길에서는 멈추지 않습니다. 문 안에서는 조작을 요구하지 않습니다 — 거긴 읽는 자리입니다.
- **문을 하나도 안 열고 끝까지 달려도 이력은 완결됩니다.**
- 게임 안에 프로젝트 설명을 **복제하지 마세요.** `RECS` 인덱스로 `.rec`을 꺼내 씁니다.

## 5. 배치 — 이 표가 유일한 출처

전체 2450 단위, 초당 27 → **약 90초.**

| z | 무엇 | 비고 |
|---:|---|---|
| 0 | 1장 · 2014 | 무리와 같은 길 |
| 230 | 2장 · 2015~ | |
| 470 | 3장 · 2020 | **가로등이 여기까지만 켜져 있음** |
| 700 | 4장 · 2025 | 유입이 꺼진 구간 |
| **880** | **5장 · 갈림길 시작** | 무리가 오른쪽으로 당김 |
| 1050 | 갈림길 완료 | 두 도로가 완전히 분리 |
| **1090** | **포장도로 끝** | 여기까지 안 내려서면 멈춤 |
| 1160 | 6장 · 2025-12 | |
| 1180 | 문 1 · **aeo_sync** → `↓ 발행` 해금 | 첫 도구 |
| 1360 | 문 2 · MediSTT → 자막 | |
| 1460 | 7장 · 2026-05 | |
| 1520 | 문 3 · MetaPorter → **인용 계기판 (눈금이 `?`)** | |
| 1680 | 문 4 · SERP Sentinel → 경쟁 레이더 | |
| 1800 | 문 5 · KPIY → KPI 그래프 | |
| 1900 | 8장 · 2026-07 | |
| **1930** | **문 6 · aeo-log-analyzer → 랜턴** | **전환점** |
| 2140 | 9장 · 2026-08 | |
| 2160 | 문 7 · NetTier → 국경 개방 | |
| 2260 | 국경 | |
| 2340 | 10장 | |
| **2350** | **빈 문틀 (Price Lock)** | 유일하게 상호작용 불가 |
| 2400 | 문 8 · Hospify → 도착 | |
| 2440 | 병원 | |
| 2450 | 끝 | |

**1~5장이 전체의 36%입니다.** 12년이고, 이 자기소개의 절반입니다. 줄이지 마세요.

### 절대 건드리지 말 것 — 랜턴은 여섯 번째

1~5번 문을 지나는 동안 **크롤러는 안 보이고 인용 계기판은 `?`에 멈춰 있습니다.**
그 구간이 실제 7개월입니다. 여섯 번째 문에서 화면이 한 번에 드러나고 `?`가 숫자로 바뀌는
그 순간이 이 게임의 전부입니다. 앞으로 당기면 8장이 통째로 무너집니다.
`?selftest`가 이 순서와 `?` 동작을 검사합니다.

---

## 6. 캐릭터

### 주인공 = 최희준

도구를 의인화한 게 아닙니다. 플레이어가 조작하는 사람이 곧 저입니다.
화면 문장이 전부 1인칭인 것도 여기서 나옵니다 — "이 도구는 ~합니다"가 아니라 "저는 ~했습니다".

### 뒷모습이라 못 쓰는 것

**안경 · 표정 · 얼굴 · 재킷 앞면 · 정면 실루엣.** 전부 사람의 반대쪽에 있습니다.
예전 픽셀 버전의 안경(앰버)·가방(블루)·재킷(아이보리)은 옆모습 기준이라 그대로는 못 씁니다.

### 뒤에서 읽히는 것 — 이것만 씁니다

| 단서 | 어떻게 쓰나 |
|---|---|
| **가방** | 등 한가운데. 뒤에서 정체성을 나를 수 있는 유일한 실루엣 |
| **손에 든 것** | 여섯 번째 문 이후 램프. 팔 스윙을 따라 흔들림 |
| **림라이트** | 왼쪽 가장자리에 앰버 한 줄. 나만 있음 |
| **달리는 폼** | 뒤꿈치 차올림 · 무릎 들림 · 어깨/골반 역회전 · 팔꿈치 벌어짐 · 상하 바운스 |

옆모습 사이클을 뒤에서 본 것처럼 그리면 **서 있는 것처럼 보입니다.**
앞뒤 보폭은 뒤에서 보면 정면이라 안 보입니다. (`docs/design/README.md`에 실패 두 번 기록)

### 확정된 캐릭터 시트

```
주인공   실루엣 #080A10 · 왼쪽 림라이트 rgba(255,190,90,1)
         + 백팩(앰버 테두리 rgba(255,174,26,.62), 뚜껑 이음선 한 줄)
         + 램프(여섯 번째 문 이후, 오른손)
무리     실루엣 #39415C · 림라이트 없음 · 가방 없음 · 램프 없음
키       1.75 월드 단위 (양쪽 동일)
```

**두 사람의 유일한 차이는 가방입니다.** 같은 릭, 같은 실루엣, 같은 폼.
한 사람만 뭔가를 지고 있습니다.

### 가방이 성장 곡선입니다

문을 지날 때마다 가방이 커집니다 — `0.18 → 1.0`, 문 8개에 걸쳐서.
**성장이 UI 숫자가 아니라 실루엣에 보이는 게 요점입니다.**
1장에서 아무것도 못 하는 것과, 10장에서 등이 무거운 것이 같은 그림의 양 끝입니다.

무리는 끝까지 빈손입니다. 그게 5장입니다.

### 아직 안 정한 것

- **얼굴을 한 번이라도 보여줄 것인가** (엔딩에서 돌아본다든지). 지금은 안 보여줍니다.
- 가방 안에 뭐가 들었는지 보여줄 것인가 (열린 문 8개 = 8개의 무엇).
- 램프 외에 손에 드는 것을 더 늘릴 것인가.

---

## 7. 월드 구성물

| 물건 | 무슨 뜻인가 | 규칙 |
|---|---|---|
| **계측 그리드** | 화풍 B. "당신은 계측기 위를 달리고 있다" | 눈금은 **월드 z에 고정**. 깊이에 고정하면 안 흐름 |
| **가로등** | **유입** | z<520 켜짐 → 꺼짐 → **발행한 자리 반경 120에서만 다시 켜짐** → 국경 개방 후 전부 |
| **포장도로** | 업계가 가는 길 | 갈림길 이후 어두워지다 z 1350에서 끊김 |
| **내 길** | 갈림길에서 왼쪽 8.5 단위로 분리 | 두 도로 폭 합보다 넓어야 겹치지 않음 |
| **크롤러** | AI 봇 | **랜턴 전 alpha .07** — 있는데 못 셈. 페이지로 내려앉으면 인용 +1 |
| **페이지** | 발행물 | 앞쪽 40에 심음. 최소 간격 26 → **달리면서만 발행 가능** |
| **문** | 프로그램 8개 | **길 전체 폭**. 못 지나칠 수 없음 |
| **국경** | NetTier 전까지 벽 | 열리면 앰버 문틀 |
| **빈 문틀** | Price Lock. 만들어 놓고 지움 | 상인방 가운데가 끊겨 있음. **절대 안 열림** |
| **병원** | 도착 | 국경 열린 뒤 사람들이 걸어 들어감 |

가로등 규칙이 이 게임의 인과 그 자체입니다 — **발행한 자리에만 불이 돌아옵니다.**

## 8. 화면 문장

- **한 번에 한 줄, 짧게.** 캔버스가 아니라 **DOM 오버레이**입니다(폰트·접근성).
- 크기 `clamp(1.5rem, 5.2vw, 3.3rem)`, 화면 아래 9%, 위로 그라데이션 스크림.
- **`docs/narrative.md`에 없는 문장은 화면에 없습니다.** 필요하면 거기에 먼저 씁니다.
- `?selftest`가 장면 문장 길이 26자 이하를 검사합니다 — 길면 큰 글자로 안 들어갑니다.

## 9. 소리 · 접근성

- 오실레이터 블립 여섯 개, 파일 없음. **기본 꺼짐** — 예고 없이 소리 내는 이력서는 실례입니다.
- `prefers-reduced-motion`을 지키면 흔들림과 먼지가 꺼집니다.
- 진행은 `#jn-live`(`aria-live=polite`)로 안내합니다. 같은 내용이 아래 본문에 전부 글로 있습니다.
- 터치 패드는 `aria-hidden` 안에 두지 마세요 — 포커스 가능한 요소가 들어가면 WCAG 4.1.2 위반입니다.

## 10. 검증

```
node test/walk.js                    # 1장부터 엔딩까지 실제로 달려본다
?selftest                            # 상수·배치·순서·투영을 검사
```

`?selftest`가 지키는 것: 투영이 실제로 수렴하는가 · 두 도로가 겹치지 않는가 ·
**조작 속도가 무리의 당김을 이기는가** · 문 순서가 커밋 순서인가 · 랜턴이 여섯 번째인가 ·
`?`가 랜턴 전에만 뜨는가 · 발행이 앞쪽에 심기는가 · 가로등이 발행한 자리에서만 켜지는가 ·
장면 문장이 큰 글자에 들어가는 길이인가.

`test/walk.js`가 잡는 건 다른 종류입니다 — **끝까지 갈 수 있는가.**
실제로 갈림길에서 `←`를 붙잡았을 때 조작 클램프에 밀려 갈림길이 성립 안 되던 버그를 이게 잡았습니다.

## 11. 아직 안 정한 것

- **하늘.** 지금 비어 있습니다. 별·구름·건물 스카이라인 중 무엇을 넣을지.
- **하늘색이 막마다 바뀔 것인가** (밤 → 새벽). 지금은 고정입니다.
- **밝기.** 문을 지날 때마다 밝아지는데(0.30 → 0.02), 시작이 너무 어두운지.
- **점수 숫자를 계속 보여줄 것인가.** 게임 점수와 실제 실적 수치가 같은 모양으로 보여서
  헷갈릴 수 있다는 지적이 있었습니다. (`docs/game-concept.html` Q1)
- **모바일에서 어디까지 할 것인가.**
- 캐릭터 미확정 항목 세 개 (위 6절).
