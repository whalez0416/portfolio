# portfolio

최희준(의료 마케팅 × 소프트웨어) 개인 포트폴리오. 채용·영업 양쪽에 보여주는 용도.

## 무엇인가

**의존성 0의 단일 HTML 한 장.** 빌드도, CI도, 패키지도 없습니다.
`index.html` 안에 마크업 · CSS · 게임 · 로그 분석기가 전부 들어 있습니다.
이 제약은 자랑거리이자 설계 원칙입니다 — 외부 스프라이트시트나 CDN 라이브러리를
추가하기 전에 반드시 다시 생각할 것. (폰트만 예외이고, 차단 시 시스템 폰트로 폴백)

배포는 `main` 푸시 → GitHub Pages. https://whalez0416.github.io/portfolio/

## 톤

앰버(`--sig`) 온 니어블랙의 계측기/콘솔 미감. 화려함이 아니라 "읽히는 계기판"입니다.
문장은 담백한 1인칭 서술체 — 과장 금지, 수치는 저장소 커밋 이력에서만 인용.
서버 접속정보와 클라이언트 내부 실적 수치는 절대 넣지 않습니다.

## 본문은 한 벌뿐

8개 프로젝트 기록은 `.rec` 마크업이 유일한 출처입니다.
게임은 `RECS` 인덱스로 그 마크업을 꺼내 쓰기 때문에, 기록을 고치면 게임이 따라옵니다.
**게임 안에 프로젝트 설명을 복제하지 마세요.**

## 문서가 먼저, 순서대로

```
docs/narrative.md   무슨 이야기인가 (열 장면, 화면에 뜰 문장)
docs/programs.md    문 여덟 개가 무슨 말을 하나
docs/design/        어떻게 보이나 (화풍 B, 인물 러닝 사이클)
docs/game-design.md 어떻게 돌아가나 (시점·조작·배치·캐릭터)   ← 게임을 만지기 전에 반드시
index.html          코드
```

**위에서 아래로만 흐릅니다.** 문서와 코드가 다르면 코드가 틀린 겁니다.
`docs/game-design.md`가 없던 동안 서사만 보고 옛날 엔진에 얹는 사고가 한 번 났습니다 —
확정된 시점(등 뒤)이 반영되지 않은 채로 커밋이 여덟 개 올라갔습니다.

`docs/narrative.md`가 기준입니다. 이건 자기소개라 **게임의 주인공은 도구가 아니라 최희준**입니다.
한 줄은 "저는 파는 사람이었습니다. 팔던 것이 죽는 걸 보고, 팔 것을 직접 만들기 시작했습니다."
문장은 전부 1인칭으로 씁니다 — "이 도구는 ~합니다"가 아니라 "저는 ~했습니다".

게임은 **두 갈래**입니다. 섞지 마세요.

| | 무엇 | 어디 |
|---|---|---|
| **길** | 서사 — 사람 이야기. 걸으면 열 장면이 흐름 | `docs/narrative.md` |
| **문** | 프로그램 — 만든 이유 · 막힌 곳 · 판단 | `docs/programs.md` |
| **기록** | 사실 — 수치와 스택 | `index.html`의 `.rec` 한 벌 |

문은 선택입니다. 하나도 안 열고 끝까지 걸어도 이력은 완결됩니다.
길에서는 멈추지 않고, 문 안에서는 조작을 요구하지 않습니다.
문 안의 본문은 `RECS` 인덱스로 `.rec`을 꺼내 씁니다 — 기능 설명을 복제하지 마세요.

도구 순서는 커밋 날짜입니다. **계측기(aeo-log-analyzer)는 첫 번째가 아니라 여섯 번째**입니다 —
7개월을 만들고 나서야 증명이 안 된다는 걸 알고 만든 것이라, 앞으로 당기면 8장이 무너집니다.

**화면에 뜨는 큰 글자는 전부 그 문서에 있는 문장이어야 합니다.**
새 문장이 필요하면 거기에 먼저 쓰고, 게임은 거기서 잘라냅니다 —
메커니즘을 만들고 문장을 갖다 붙이는 순서로 두 번 갈아엎었습니다.
자기소개이므로 없었던 일은 지어내지 않습니다.

## 게임의 구조 (상세는 `docs/game-design.md`)

**등 뒤에서 보는 시점 하나로 사막을 걸어 지평선의 산까지.** WebGL2 3D입니다.
조작은 `↑` 걷기와 `← →` 방향 **두 개뿐**이고, **정해진 길이 없습니다** —
자동 이동은 멀미의 첫 원인이라 걷어냈습니다. 되돌리지 마세요.

**시점은 절대 바뀌지 않습니다.** 예전 버전은 위에서 내려다보다가 갈림길에서 옆으로
눕혔는데, **두 개의 게임처럼 느껴진다**는 이유로 폐기됐습니다. 되돌리지 마세요.
무리는 자막 지점에서 한꺼번에 꺾어 가고 한 명이 멈춰 돌아봅니다 — 그게 갈라짐입니다.

점프 없고, 죽지 않고, 리셋이 없고, 막는 벽도 없습니다. 자기소개라 그렇습니다.

**어지럽지 않게 만드는 것이 이 게임의 절반입니다.** 카메라는 지형을 1:1로 따라가지 않고
평균 높이를 늦게 좇으며, 시선은 인물이 아니라 앞쪽 한 점을 봅니다. 화각은 44도입니다.
지형 격자는 **일정 간격이어야 하고 카메라를 칸에 스냅**해야 합니다 — 안 그러면 플랫셰이딩
면이 매 프레임 갈아치워지며 바닥이 기어다닙니다. 격자는 **뒤로도 200 단위** 깔립니다
(40이면 뒤돌아볼 때 지나온 유적이 하늘에 뜹니다).

유적 여덟은 커밋 날짜 순으로 산 쪽에 놓입니다. `TEMPLES` · `SCRIPT` 배열이 유일한 배치 출처입니다.
세상의 모든 것은 `(x, z)` **월드 단위**입니다.

**미술의 원본은 `docs/design/04-journey-3d.html`이고, 엔진은 거기서 `index.html`로 옮겨온
것입니다.** 프로토타입만 고치고 끝내면 배포되는 화면은 그대로입니다 — 실제로 21커밋 동안
그랬습니다. 고치면 **반드시 양쪽과 `docs/game-design.md`를 같이** 맞추세요.

**캔버스에 `tabindex="0"`이 있어야 합니다.** 없으면 키보드로 한 걸음도 못 걷습니다.

**절대 건드리지 말 것 — 계측기는 여섯 번째입니다.** 앞으로 당기면 게임이 무너집니다.
1~5번 유적을 지나는 동안 크롤러(앰버 티끌)는 하나도 안 보입니다.
그 구간이 실제 7개월이고, 여섯 번째 유적에서 나오며 사막 전체가 켜지는 순간이 이 게임의 전부입니다.
`?selftest`와 `test/walk.js`가 이 순서를 검사합니다.

## 화면이 곧 주장

**랜턴 없이는 크롤러가 안 보입니다** = "측정되지 않던 것을 계측한다".
여섯 번째 유적에서 **나오는 순간** 사막 전체에 앰버 티끌이 떠오르고, 그 전에는 하나도 없습니다.
그 구간이 실제 7개월입니다.

게임을 멈추고 텍스트 모달을 띄우는 방식으로 되돌아가지 마세요 — 그건 게임이 아니라
목차가 됩니다. 상세는 유적 안과 엔딩 링크로만 엽니다.

인물은 스프라이트가 아니라 **코드로 조각한 3D**입니다. 회전체가 아니라 사다리꼴 로브 +
각진 크리스 + 열린 옷단 + 어깨 단차입니다. 법선을 정점에 안 싣고 프래그먼트에서 미분으로
구하므로 지형도 로브도 스카프도 **같은 셰이더**로 플랫셰이딩됩니다.
IK는 일부러 안 씁니다 — 실패 두 번이 `docs/design/README.md`에 있습니다.

## 검증

```
node test/walk.js              # 끝까지 갈 수 있는가 (브라우저 없이)
node test/a11y.js              # 진짜 브라우저에서 접근성이 성립하는가
node test/shoot.js             # 실제로 어떻게 보이는가 (프레임 촬영)
?selftest                      # 상수·배치·순서·로그 분석기 분류
python -m http.server 8000     # 로컬 (반드시 --directory 로 이 폴더만)
```

게임을 만졌으면 **셋 다** 돌릴 것 — 서로 다른 종류의 실패를 잡습니다.
`?selftest`는 배치와 순서를, `walk.js`는 "끝까지 갈 수 있는가"를, `shoot.js`는 그림을 잡습니다.
**`shoot.js`가 없으면 그림 문제는 절대 안 잡힙니다** — 두 번 갈아엎은 이유가 전부 그림이었습니다.
브라우저 검사는 `npm i -g puppeteer-core`가 필요합니다(저장소에는 안 들어갑니다).

WebGL 캔버스는 `preserveDrawingBuffer` 없이 `drawImage`로 읽으면 늘 비어 나옵니다 —
**픽셀을 직접 세는 검사는 언제나 "검은 화면"이라고 거짓말합니다.** 찍힌 PNG 크기로 보세요.

파일은 CRLF · UTF-8입니다.
