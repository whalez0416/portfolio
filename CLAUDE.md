# portfolio

최희준(의료 마케팅 × 소프트웨어) 개인 포트폴리오. 채용·영업 양쪽에 보여주는 용도.

## 무엇인가

**의존성 0의 단일 HTML 한 장.** 빌드도, CI도, 패키지도 없습니다.
`index.html` 안에 마크업 · CSS · 횡스크롤 게임 · 로그 분석기가 전부 들어 있습니다.
이 제약은 자랑거리이자 설계 원칙입니다 — 외부 스프라이트시트나 CDN 라이브러리를
추가하기 전에 반드시 다시 생각할 것. (폰트만 예외이고, 차단 시 시스템 폰트로 폴백)

배포는 `main` 푸시 → GitHub Pages. https://whalez0416.github.io/portfolio/

## 톤

앰버(`--sig`) 온 니어블랙의 계측기/콘솔 미감. 화려함이 아니라 "읽히는 계기판"입니다.
문장은 담백한 1인칭 서술체 — 과장 금지, 수치는 저장소 커밋 이력에서만 인용.
서버 접속정보와 클라이언트 내부 실적 수치는 절대 넣지 않습니다.

## 본문은 한 벌뿐

8개 프로젝트 기록은 `.rec` 마크업이 유일한 출처입니다.
게임은 `RECS` 인덱스로 그 마크업을 꺼내 쓰기 때문에, 기록을 고치면 게임이 따라옵니다.
**게임 안에 프로젝트 설명을 복제하지 마세요.**

## 서사가 먼저

`docs/narrative.md`가 기준입니다. 명제는 "추정은 틀립니다. 그래서 전부 재는 쪽으로 만들었습니다."
**화면에 뜨는 큰 글자는 전부 그 문서에 있는 문장이어야 합니다.**
새 문장이 필요하면 거기에 먼저 쓰고, 게임은 거기서 잘라냅니다 —
메커니즘을 만들고 문장을 갖다 붙이는 순서로 두 번 갈아엎었습니다.

## 상단 게임

횡스크롤 플랫포머. 핵심 원칙은 **조작법이 곧 주장**이라는 것입니다 —
도구를 주울 때마다 동사가 하나씩 열리고, 그 동사가 그 도구의 주장입니다.
(랜턴 없이는 크롤러가 안 보임 = "측정되지 않던 것을 계측한다")

1막에서 플레이어가 아무것도 못 하는 것은 버그가 아니라 전제입니다.
게임을 멈추고 텍스트 모달을 띄우는 방식으로 되돌아가지 마세요 — 그건 게임이 아니라
목차가 됩니다. 상세는 엔딩 링크와 `Enter`로만 엽니다.

캐릭터·크롤러·페이지는 코드로 그린 픽셀 스프라이트(`bake()` + ASCII 맵)입니다.

## 검증

`?selftest`를 붙이면 콘솔에서 검증됩니다. 로그 분석기 분류와 게임 밸런스
(세 레인의 난이도 구배, 발판 착지, 재크롤 대기, 도구↔기록 이름 일치)를 확인합니다.
게임 상수를 만졌으면 반드시 이걸로 확인할 것 — 특히 `LANES` · `JUMPV` · `GRAV` ·
`HOVER`는 서로 물려 있어서 하나만 바꾸면 레인 난이도나 인용 루프가 조용히 죽습니다.

```
https://whalez0416.github.io/portfolio/?selftest
python -m http.server 8000     # 로컬
```

파일은 CRLF · UTF-8입니다.
